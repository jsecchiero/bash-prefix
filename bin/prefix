#!/bin/bash


prefix() {

    export pre=${@}
    export PS1="$pre> "

    [[ -f ../lib/bash-preexec/bash-preexec.sh ]] && source ../lib/bash-preexec/bash-preexec.sh
    preexec() { $pre $1; kill -INT $$; };
    export -f preexec
}

export -f prefix

bash --rcfile <( echo prefix $@ )
